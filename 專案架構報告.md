# 7L AI VTuber — 專案架構研究報告

> 產出日期：2026-09-22
> 研究範圍：`C:\Users\CPXru\Desktop\thumb\大拇哥實驗室\ai_vtuber`
> 方法：靜態通讀原始碼 + 依賴交叉比對 + `git log` + runtime JSON 結構檢視（未實際啟動）

---

## 1. 一句話結論

這是一個**單一 Python 程序、以 asyncio 為核心的 Windows AI VTuber 直播中樞**：把「麥克風、TikTok 彈幕、鍵盤、YouTube 畫面、系統畫面」等 5 種以上的輸入塞進一個 `asyncio.Queue`，由一個 7,881 行的主檔統一排程約 30 個背景 worker，經 LLM（Groq 文字主力 + Gemini 視覺/Live）產生帶動作標記的文字，再同時輸出到 VTube Studio 表情/口型、TTS 語音、Web 控制台與字幕。

**沒有框架、沒有測試、沒有模組邊界設計**——是典型的「先能跑再說」個人專案，但功能覆蓋面極廣，且已含 RAG、向量庫、聲紋辨識、MIDI 演奏、翻唱管線等相當完整的子系統。

---

## 2. 規模與檔案分佈

| 項目 | 數值 |
|---|---|
| 主力 Python 檔（不含 scratch / playground） | 61 檔、約 30,002 行 |
| `scratch/` 實驗與除錯腳本 | 60 檔 |
| 最大單檔 | `vts_7L_test.py` 7,881 行 |
| 第二大單檔 | `test_virtual_piano.py` 2,465 行（鋼琴子視窗本體） |
| 第三大 | `services/piano_engine.py` 2,285 行 |
| Git 歷史 | 14 commits，**全部在 2026-09-22 同一天**（今日初始匯入 + 同日修補） |
| 未提交變更 | 6 個 `data/*.json` runtime 狀態檔 |

### 目錄職責

```
ai_vtuber/
├─ vts_7L_test.py        ← 主程式（唯一的直播中樞入口）
├─ core/                 ← 大腦：LLM 路由、記憶、Prompt、DB、工具函式
├─ services/             ← 四肢：TTS、VTS、TikTok、Web、鋼琴、RAG、翻唱
├─ mic_live_plugin/      ← 感測：音訊分析、聲紋、Windows 桌面感知
├─ realtime_tasks/       ← 即時狀態廣播（本地 JSON + Firestore）
├─ vercel-router/        ← 雲端 Groq 金鑰轉發器（Node/Vercel Serverless）
├─ web/                  ← Web 控制台前端（index.html，32.5 KB 單檔）
├─ data/                 ← 所有 JSON 狀態、RAG 向量庫、快取
├─ knowledge/            ← 外部知識庫（.md/.txt → 灌進 RAG）
├─ models/               ← Kokoro TTS 模型（325MB onnx + 54MB voices）
├─ midi_sheets/          ← MIDI 曲目目錄
├─ scripts/              ← RAG 建檔、VTS token 領取、TTS 微調、語料產生
├─ scratch/              ← 一次性測試與除錯（不參與主路徑）
├─ *.bat                 ← 各種啟動器（見第 3 節）
└─ .env / 6.env          ← 金鑰（已列 .gitignore）
```

---

## 3. 啟動方式：誰是真正的入口

| 啟動器 | 執行 | 角色 |
|---|---|---|
| **`V7.bat`** | `python vts_7L_test.py` | ✅ **主入口**，完整直播系統。會自動開 Windows Terminal |
| `7L.bat` | `python 7L.py` | ⚠️ 舊版 Discord Bot（獨立，1,891 行），非主路徑 |
| `bot.bat` | 載 `6.env` 後跑 `bot.py` | 多帳號 Discord Bot 工廠（6 個 token，`bot_factory()`） |
| `run_dashboard.py` | 直跑 dashboard | 僅開控制台（需主程式已起，否則 callback 為空） |
| `load.bat` | 僅檢查/安裝依賴 | 系統健檢 |
| `start_macro.bat` | `auto_clicker_tool.py` | PyQt6 熱鍵巨集工具（獨立） |
| `🌸_7L_NDI透明接收器.bat` / `🎹_7L_鋼琴NDI...bat` | NDI 接收器 | 獨立透明視窗，用於跨 VM/跨機畫面疊加 |
| `🎙️_錄製老爸純淨聲紋.bat` | `record_clean_dad_voiceprint.py` | 錄製老爸聲紋锚點 |

**首次啟動**：`7L.bat` / `bot.bat` 會檢查 `.deps_ok` 標記檔，沒有就先 `pip install -r requirements.txt`，並在非零結束時每 10 秒自動重試（針對 Discord 500/503）。

### `vts_7L_test.py` 的 `async main()` 啟動序列（L7926–8567）

```
init_piano_synthesizer (thread)      ← 鋼琴音色預載
print_model_parameters(vts)
ai_face_tracking_loop + vts_health_worker
_prewarm_tts (thread)                ← TTS 引擎預熱
──────── 以下約 25 個 asyncio.create_task ────────
mic_volume_worker        ← 麥克風音量條
mic_worker               ← STT 聽寫
text_file_listener_worker← 讀 chat_input.txt（除錯注入）
console_keyboard_input_worker ← 鍵盤直噴
screen_capture_worker    ← dxcam 畫面截取
chat_processor_worker    ★ 中央輸入分派
streamer_mind_loop_worker★ 觀眾留言佇列消化
proactive_worker         ← 主動開口（45~75s 間隔）
background_mind_stream_worker ← 背景思緒流（1.2~2.5s）
anti_watermark_worker / cma_monitor_worker
peripheral_vision_worker ← 周邊視覺
speech_queue_worker      ★ 語音輸出排程
system_audio_worker      ← WASAPI loopback 抓系統聲
expression_keeper_worker / autonomous_wander_worker
piano_focus_udp_worker / piano_liveness_watchdog_worker
tiktok_live_worker       ★ TikTok 彈幕
auto_restore_piano_state_on_startup
live_timer_sensor_worker
safe_discord_runner
────────────────────────────────────
web_dash.start_web_dashboard(port=7860, ...15 個 callback)
yt_comp.init_yt_companion()          ← YouTube 陪伴模式
while True: 顯示狀態列 (0.1s 更新)
```

主執行緒最後只做**狀態列印**（ANSI `\r` 原地刷新），所有實際工作都在背景 task。

---

## 4. 核心資料流

### 4.1 輸入 → 佇列 → 分派

所有來源統一寫入 `input_queue`（L7968 `asyncio.Queue`），項目為：
```python
{"text": str, "audio_base64": str|None, "timestamp": float, "source": str}
```

`chat_processor_worker`（L7091）依 `source` 分流：

```
source 分派表（L7118–7270）
├─ 立即指令     /shock /punish /reward  → 直接執行，不進 LLM
├─ IS_SLEEPING  睡眠模式 → 只認喚醒詞，其餘丟棄（0 API 呼叫）
├─ "mic" / "text_file" / "console" / "web_console" → 爸爸管道
│     → asyncio.create_task(process_chat_message(..., source=...))
├─ "tiktok"（一般觀眾）→ 判斷是不是爸爸帳號
│     ├─ 是 → process_chat_message(..., source="tiktok_dad")
│     └─ 否 → add_to_streamer_mind_board() 進觀眾待讀佇列
├─ "tiktok_ambient"（入場/點讚/禮物等氛圍事件）
│     → 只更新狀態列；若正在說話/彈琴則靜默丟棄
└─ 其他 → process_chat_message
```

> **設計重點**：一般觀眾留言**不會立即觸發 LLM 呼叫**，而是進 `STREAMER_MIND_BOARD`，由 `streamer_mind_loop_worker`（L6331）每 1 秒檢查、在空閒時用 Gemini 梯隊（3.1 Flash Lite → 3.5 → 3.7）**一次讀 100 句**批量消化。這是控制 API 成本的關鍵設計。

### 4.2 一次對話的完整生命週期

`process_chat_message`（L6683）：

```
1. get_user_profile()                    讀觀眾檔案
2. evaluate_memory_demand(user_input)    ★ 五級記憶需求判定
     INSTANT / LIGHT / STANDARD / DEEP / ULTRA_DEEP
     → 回傳 (u_lim 對話句數, h_lim 歷史筆數, th_lim 思緒字元數)
3. get_cloud_knowledge()                 讀人格/meme/自學知識
4. build_rag_section(user_input)         ★ RAG 向量檢索（chromadb）
5. fetch_from_long_term_memory(...)      讀 Firestore 歷史
6. judge_subconscious_intent_via_live_api 潛意識意圖判讀（可選）
7. get_unified_memory_context(u_lim, th_lim) 組裝記憶區塊
8. PromptTemplateEngine.build_chat_system_prompt(...)  ★ 組 System Prompt
9. fetch_ai_response(messages)           ★ LLM 主呼叫
10. execute_actions(vts, bot_reply,...)   解析動作標記 → VTS
11. speech_queue.put({...})              進語音佇列
12. save_to_long_term_memory(...)        寫回 Firestore
```

### 4.3 文字 LLM 的雙梯隊

**第一梯隊：Groq**（`core/groq_router.py`，449 行）

```python
DEFAULT_MODEL_LADDER = [
    "qwen/qwen3.8-27b",      # 最快主勞（首選，~0.4s）
    "openai/gpt-oss-120b",   # 複雜推理
    "openai/gpt-oss-20b",    # 通用平衡
    "allam-2-7b",            # 最輕量（阿拉伯文向，備援）
]
```
- `.env` 有 **16 個 `GROQ_API_KEYS`**，round-robin 建 `AsyncGroq` client
- 逐 key / 逐 model 冷卻表（429 → 依 header 冷卻；其他 → 60s）
- 啟動時呼叫 Groq `/models` **交叉驗證梯隊**，過濾掉帳號不存在的模型（6 小時 TTL 快取，防 404）
- 回傳 **Gemini 相容殼 `GroqResponse`**（`.text` / `.function_calls`），並自動把 Gemini `types.Schema` 轉 OpenAI wire format、還原 `pe.play_virtual_piano` 這類帶點號的工具名

**快速反射（Reflex）**：`fetch_fast_text_reply`（L1454）
先打 `groq_chat`（timeout 短），失敗才退到 Gemini 梯隊 `3.5-flash-lite → 3.6-flash → 3.1-flash-lite → 3-flash-preview`。目標是「第一時間接住話」。

**第二梯隊：Gemini**（`core/llm_engine.py`）
- `.env` 的 `GEMINI_API_KEY` 被切成**功能分區金鑰池**：

| 池 | 切片 | 用途 |
|---|---|---|
| `KEYS_AUDIENCE_LIVE` | `[0:6]` | 觀眾 Live 回覆 |
| `KEYS_MIC_LIVE` | `[6:12]` | 麥克風 Live |
| `KEYS_VISION` | `[12:18]` | 視覺理解 |
| `KEYS_PROACTIVE` | `[18:24]` | 主動發言 |
| `KEYS_MIND_LIVE` | `[24:30]` | 思緒流 |
| `KEYS_DAD_MAIN` | 全部 | 爸爸主對話 |

- `DualHotStandbyLiveManager`：每個通道維持 **主 + 待命** 兩把金鑰，主 key 失敗立即切待命並找下一把健康 key
- `get_prioritized_gemini_models()`：依 query 有無圖片/是否 proactive 排模型優先序
- `record_model_failure()` + `DEAD_GEMINI_MODELS`：壞模型記名排除

**第三出路：Vercel 轉發器**（`vercel-router/`，Node Serverless）
28 個 Groq key 部署在 Vercel，對外一個 endpoint、對內輪替。逐 key 冷卻規則：`429→60s`、`401/403→10min`、`5xx→30s`。回 `x-router-model` / `x-router-key-index` header 供除錯。建議 region `hkg1`（香港）或 `sin1`（新加坡）。

### 4.4 System Prompt 組裝（`core/prompts.py`，1,025 行）

`PromptTemplateEngine.build_chat_system_prompt()`（L1007）動態拼接：

```
[統一時間+Tick 狀態]      ← core/utils.py（uptime / 靜默秒數）
[鋼琴即時狀態]            ← piano_engine.get_piano_realtime_prompt()
[OS 桌面遙測]             ← os_desktop_sensor.build_os_telemetry_prompt()
[階段 2 指令]
[雲端知識區塊]            ← cloud_knowledge_local.json
[統一記憶區塊]            ← 記憶需求判定決定長度
[HARD_TECHNICAL_RULES]    ← L822 硬規則（防跑版）
[當前對話對象 / 語音情緒 / 系統標籤 / 印象]
```

**回覆中的動作標記**（由 `parse_action_timeline` L3352 解析）：

| 標記 | 作用 |
|---|---|
| `[EXPRESSION: 開心]` | 觸發 VTS 表情 |
| `[SPEED:+10%]` / `[PITCH:-2Hz]` | 即時語速/音高 |
| `[SILENCE]` | 停頓（主動發言的「收尾」手段） |
| `[THOUGHT: ...]` | 內心獨白，抽出不唸出來 → `thought_memory` |
| `[NEW_NAME: xxx]` / `[NEW_IMPRESSION: ...]` | 觀眾改名/更新印象 → 寫 viewer profile |
| `[WINK]` `[TIMER: 300\|訊息]` 等 | 眨眼、倒數計時 |

`TextCleanEngine`（L20）負責：`extract_thought` 抽獨白、`deduplicate_intra_reply_sentences` 去重、`natural_clause_segmentation` 斷句、`fix_heteronyms_for_tts`（L334，用 `data/tts_heteronym_dictionary.json` 修多音字）、`japanese_to_xiaoyi_phonetic`（日文轉音譜）。

> 依 commit `57fee7b`：Kokoro 中文改用 misaki `version=1.1` 前端（注音+數字調），修正中文被唸成粵語的問題。

### 4.5 記憶系統（`core/memory.py`，827 行）

**五層記憶**：

| 層 | 檔案 | 內容 | 大小 |
|---|---|---|---|
| 對話記憶 | `data/dialogue_memory.json` | 誰說了什麼、時間、角色 | 3 KB |
| 統一記憶 | `data/unified_memory.json` | 主記憶佇列 | 116 KB |
| 思緒記憶 | `data/thought_memory.json` | 7L 內心獨白 | 93 KB |
| 觀眾檔案 | `data/viewer_profiles_local.json` | `tikname/call/relationship/impression/last_seen` | 13 KB |
| 雲端知識 | `data/cloud_knowledge_local.json` | persona、自學 facts、meme、自訂規則 | 3 KB |

**記憶需求五級判定** `evaluate_memory_demand()`（L729）——依輸入關鍵字決定要撈多少：

| 級別 | 觸發情境 | 對話句數 | 歷史 | 思緒字元 |
|---|---|---|---|---|
| `INSTANT` | 寒暄/指令/快速事件 | 0 | 0 | 0 |
| `LIGHT` | 簡單問答 | 少量 | 少量 | 少 |
| `STANDARD` | 一般對話 | 標準 | 標準 | 標準 |
| `DEEP` | 過往回憶/深入話題 | 100 句 | 多 | 1000 |
| `ULTRA_DEEP` | 「上次說過…」類追溯 | **180 句** | 80 | 1200 |

`get_unified_memory_context()`（L826）組成兩段：📕 最近對話（`[時間] 🔵[觀眾]：...`）+ 🧠 近期思緒（以**字元數**為上限，而非句數）。

**長期記憶**：`core/db.py`（122 行）**不用 firebase-admin 套件**，而是自寫 `PureAsyncFirestoreClient` 直接打 Firestore REST API（含 `val_to_firestore` 型別編碼、`PureFirestoreDocumentRef.set/get/delete`）。singleton 由 `init_firestore(cred_dict)` 建立。

**觀眾檔案**範例：
```json
{ "小夏子": { "call": "小夏子", "relationship": "忠實觀眾",
  "impression": "似乎很喜歡阿茶", "last_seen": "2026-08-29 17:16" } }
```

### 4.6 RAG（`services/rag_store.py` + `embedder.py`）

- **向量庫**：ChromaDB 持久化於 `data/rag/chroma`（collection `knowledge`），已存在（`chroma.sqlite3`）
- **嵌入**：雙後端，`use_local()` 決定
  1. 本地 `fastembed` + `BAAI/bge-small-zh-v1.5`（~130MB，離線，CPU 5~15ms/批）
  2. Gemini `gemini-embedding-001`
  - ⚠️ **建檔與查詢必須用同一 embedder**，否則維度不對（模組 docstring 有警告）
- **來源**：`data/*.json`（記憶+雲端知識+觀眾檔）+ `knowledge/**.md|.txt`
- **建檔**：`python scripts/ingest_rag.py`（支援 `--source memory|knowledge`、`--reset`）
- **查詢**：`rag_store.search()` → `build_rag_section()`（L870，取 k=3）注入 prompt；`rag_prompt_block()` 供工具 `search_knowledge` 使用
- commit `6a7050d` 已做**段落去重與門檻校準**

> ⚠️ **`knowledge/` 目前只有 README.md，沒有任何實際知識檔**。RAG 實際只吃到 `data/*.json` 記憶。

---

## 5. 輸出端：從文字到螢幕

### 5.1 語音管線

```
bot_reply
  └→ execute_actions() 解析標記
  └→ speech_queue.put({"text", "target", "raw_text"})
  └→ speech_queue_worker (L5928)
       └→ play_voice_complete (L3525)  ★ 核心輸出函式
            ├ TextCleanEngine.clean_for_tts()     文字清洗+多音字
            ├ parse_action_timeline()             由 est_duration 拆動作軸
            ├ tts_producer_worker (L3684)         ★ 並行產音
            │    └ tts_router.get_tts_audio_bytes(text)
            │         逐句產 WAV/MP3 → audio_stream_queue
            ├ live_api_direct_sentence_emotions   ★ 句級情緒導演
            │    (Gemini Live API → 句子時長+語氣)
            ├ pygame.mixer 逐段播放 (Channel 或 music)
            │    播放中持續 poll queue，可被打斷
            └ execute_action_timeline()           ★ 同步 VTS 表情/語速
                 → set_vts_expression / inject_parameters
```

**TTS 路由器**（`services/tts_router.py`，269 行）——**三引擎降級鏈**：

| 引擎 | 實現 | 輸出 | 備註 |
|---|---|---|---|
| `kokoro`（預設） | 本地 ONNX `kokoro-v1.1-zh.onnx`（325MB）+ `voices-v1.1-zh.bin`（54MB） | `.wav` | misaki[zh] G2P、`zf_001~` 聲線、CPU/3060Ti 皆可 |
| `edge` | `edge-tts` 雲端（台灣腔女聲/日文） | `.mp3` | 免 GPU，備援 |
| `xiaoyi` | GPT-SoVITS 本地服務（`local_xiaoyi_service.py`） | `.wav` | 需 `C:\Users\qiwai\GPT-SoVITS` 存在 |

- 環境變數：`TTS_ENGINE`（逗號分隔優先序，預設 `kokoro`）、`TTS_FALLBACK`（預設 `edge,xiaoyi`）
- `get_tts_audio_bytes()`：**先試目前成功過的引擎**，失敗才依序降級；失敗原因存 `_engine_errors`，成功即記 `_active_engine`
- 語言偵測 `detect_language()` → 中/日/英各自聲線（`TTS_ZH_VOICE` / `TTS_JA_VOICE` / `TTS_EN_VOICE`）

### 5.2 VTube Studio（`services/vts_client.py`，695 行）

- `RobustVTSClient`：WebSocket `ws://localhost:8001/api/v2`，5 次重試連線、背景 `_socket_reader`、`request(req, timeout=2.0)`
- **Auth**：token 存 `vts_token.txt`，`request_authenticate_token()` 走 30 秒逾時讓使用者按 VTS 允許
  - `scripts/claim_vts_token.py`：token 存在但過期（408）時自動重新領取
- `inject_parameters(list, face_found, mode)`：口型/臉部參數注入
- 表情：`MY_CONTROLLED_EXPS`（4 個 `.exp3.json`）、`VTS_EXPRESSION_MAP`、`EXPRESSION_HOLD_SECONDS = 3.5`
- 空間定位：`BASE_VTS_MODEL_X=0.65, Y=-1.28, SIZE=-54.8` + `IS_AUTO_WANDER_ENABLED` 自動遊走
- `ai_face_tracking_loop`（主檔 L5359）：滑鼠/畫面追蹤 → 眼神、眨眼、翻白眼、驚嚇、wink、皺眉計時器

### 5.3 Web 控制台（`services/web_dashboard.py`，1,291 行 + `web/index.html` 32.5KB）

- **aiohttp**，port **7860**，由主程式以 15 個 callback 啟動
- 30+ REST 路由 + WebSocket `/ws`：

| 分組 | 路由 |
|---|---|
| 狀態 | `/api/status`、`/api/switches`、`/api/switches/toggle` |
| 記憶 CRUD | `/api/memory`、`/all`、`/update`、`/delete`、`/add`、`/clean_duplicates`、`/clear`、`/capacity` (GET/POST) |
| 視覺 | `/api/last_vision_image` |
| 控制 | `/api/send_message`、`/api/toggle_mic`、`/api/set_sleep`、`/api/trigger_expression` |
| 獎懲 | `/api/action/punish`、`/api/action/reward` |
| Prompt 編輯 | `/api/prompts` (GET/POST)、`/api/prompts/reset` |
| 工具 | `/api/tools`、`/tools/history`、`/tools/execute`、`/tools/clear_history` |
| 生命週期 | `/api/restart`、`/api/shutdown` |

- `broadcast_event()` + `broadcast_telemetry()`：主動推 `tiktok_comment`、遙測到前端
- `record_tool_call()`：工具呼叫歷史（供 `/api/tools/history`）
- 畫面縮圖：`grab_direct_screen_thumbnail()` + hash 快取，避免重複壓縮

**可開關的系統功能**（`_get_switches` L8342 / `_set_switch` L8436）：
`IS_AUTO_PIANO_ENABLED`、`IS_PIANO_AUTO_RADIO_MODE`、`IS_AUTO_WANDER_ENABLED`、`IS_PROACTIVE_SPEAK_ENABLED`、`IS_PERIPHERAL_VISION_ENABLED`、`IS_FACE_TRACKING_ENABLED`、`IS_MIC_ENABLED`、`IS_STREAM_CHAT_ENABLED`、`IS_SLEEPING`、`IS_YT_WATCHER_ENABLED`

### 5.4 工具系統（function calling）

`execute_tool_dispatch(fn_name, fn_args, caller_target, caller_user)`（L747）：

| 類別 | 工具 |
|---|---|
| 鋼琴 | `pe.play_virtual_piano`、`pe.compose_and_play_original_piano`、`pe.mashup_virtual_piano`、`pe.insert_virtual_piano`、`pe.open/pause/resume/stop_virtual_piano`、`pe.list_piano_sheets`、`pe.set_piano_volume/speed/instrument` |
| 唱歌 | `sing_song`、`stop_singing`、`stop_cover` |
| 感官 | `search_google`、`search_knowledge`(→RAG)、`generate_ai_image`、`control_microphone` |
| VTS | `trigger_vts_expression`、`move_spatial_position` |
| 系統 | `set_timer`、`set_sleep_mode`、`clear_all_memories`、`update_cloud_knowledge`、`open_browser`、`execute_local_python_code` |

> ⚠️ `execute_local_python_code` 讓 LLM 執行任意本機 Python —— 這是最大安全風險點（無沙箱，僅靠 prompt 層防護）。

---

## 6. 感測層（`mic_live_plugin/`）

| 模組 | 行數 | 功能 |
|---|---|---|
| `audio_analyzer.py` | 197 | **Gemini Native Audio Processing**：直接吃 WAV，辨識「非語音」（音樂/噪音/靜默/呼吸/鍵盤聲/犬吠）與情緒；不經 STT。金鑰 round-robin |
| `voiceprint_verifier.py` | 392 | **WeSpeaker ResNet34** ONNX（`wespeaker_zh_cnceleb_resnet34.onnx`，會自動從 sherpa-onnx release 下載）。兩個锚點：`dad_voiceprint.npy`（老爸）+ 7L 自己（由 TTS 產出校準）。`verify_is_dad()` / `verify_is_7l()` — 用於**迴音消除**（7L 自己的聲音不應被當成使用者）與 barge-in 打斷 |
| `os_desktop_sensor.py` | 275 | Win32 API + psutil，前景視窗、可見視窗清單、系統音樂資訊，組成 `build_os_telemetry_prompt()` 注入 System Prompt。含 `_ensure_desktop_access()` 自動切換 Input Desktop（跨 session 場景） |

**其他感測**（主檔內）：`screen_capture_worker`（dxcam 截圖）、`peripheral_vision_worker`（周邊視覺）、`system_audio_worker`（soundcard WASAPI loopback → 用於迴音判斷 `is_computer_audio_echo`）、`mic_worker`（faster-whisper / speech_recognition STT）。

**迴音防護三重**：`is_7l_voice_echo()`（L3138）+ `is_computer_audio_echo()`（L3230）+ `is_too_similar_to_recent()`（L3124，0.48 門檻）。

---

## 7. TikTok 直播間（`services/tiktok_listener.py`，524 行）

- 依賴 **`TikTokLive`** + **`EulerApiSdk`**（簽章，需 `SIGN_API_KEY`）
- `TIKTOK_USERNAME` 預設 `e5alr9qub2, e_7l_9`（**多帳號**，`get_target_tiktok_ids()` 解析）
- `tiktok_live_worker(input_queue)`（L49）註冊事件：
  `on_connect` / `on_room_user_seq`（觀眾數、點讚數）/ `on_disconnect` / `on_live_end` /
  **`on_comment`**（L237）/ `on_barrage` / `on_emote_chat` / `on_join` / `on_follow` / `on_like` /
  `on_share` / `on_question` / `on_sub` / `on_gift`（含 `_flush_gift_combo` 禮物連擊聚合）
- **去重三層**：`msg_id` set（上限 1000，FIFO）→ 無 msg_id 時用 `user|text` 5 秒文字快取 → 連線前 15 秒的舊訊息直接丟
- `on_comment` 同時：更新狀態列、`sys_notify()`、`web_dash.broadcast_event("tiktok_comment")`、寫入 `input_queue`
- 手修 patches：`_patched_get_ws_cookie_string` / `_patched_connect_init` / `_safe_build_webcast_uri`（繞過 ttwid cookie 與 Cloudflare）

---

## 8. 鋼琴與音樂子系統

### 8.1 架構：雙進程 + IPC

```
主程序 vts_7L_test.py
  └─ services/piano_engine.py (2,285 行)  ← 意圖解析、MIDI 搜尋、排程
       ├ get_piano_script_path() → test_virtual_piano.py
       ├ 子程序播放 (current_piano_process)
       ├ send_piano_ipc_command()  ← JSON over pipe
       ├ piano_focus_udp_worker()  ← UDP 焦點音符同步（給 NDI 疊加）
       ├ piano_liveness_watchdog_worker() ← 掉線偵測 → _mark_piano_offline()
       └ auto_restore_piano_state_on_startup() ← 從 piano_persisted_state.json 還原
子程序 test_virtual_piano.py (2,465 行) ← 虛擬鋼琴視窗 + 發聲 + 按鍵動畫
```

- **發聲**：`ClassicalPianoSoundEngine`（L259）— pygame + numpy 合成（`generate_piano_tone` L183），44100Hz，支援 `MIDI_INSTRUMENTS` 多樂器（L229）、踏板、音量/速度
- **狀態注入 prompt**：`get_piano_realtime_prompt()`（L442）→ 顯示正在彈哪首、進度，讓 LLM 能「邊彈邊聊」

### 8.2 MIDI 曲目來源：四層降級鏈

`resolve_local_midi_file()`（L1342）：

```
1. 本地目錄 midi_sheets/ + midi_catalog.json 快取比對
      └ match_midi_with_ai() / resolve_piano_intent_by_ai() (L1149)
         用 LLM 判斷「觀眾點的歌」對應哪個檔名
2. OnlineSequencer.net  (onlinesequencer_engine.py, 420 行)
      └ Chrome CDP 真瀏覽器繞 Cloudflare，session cookie 存 oseq_chrome_profile
      └ 搜尋 → Sequence ID → https://onlinesequencer.net/app/midi.php?id=<id>
3. BitMidi (bitmidi_engine.py, 124 行)
      └ 純 HTTP 抓 bitmidi.com 搜尋頁解析
4. YouTube → MP3 → AI 轉 MIDI (cloud_mp3_to_midi.py → audio_to_piano_midi.py)
      └ 自研「和聲濾波鋼琴轉譜」：
        • 消除 2x/3x/4x/5x 諧波列
        • 最大複音數限制（預設 10 聲部）
        • 瞬間 onset 偵測修正力度
        • 輸出標準 88 鍵 MIDI
```
編排層：`pianist_midi_engine.py`（141 行）依序調用上述 4 層。

**其他鋼琴能力**：`mashup_virtual_piano`（兩首 MIDI 混合，`merge_two_midi_files` L2078）、`insert_virtual_piano`（插入片段）、`compose_and_play_original_piano`（L2305，LLM 依主題即興創作譜）、`generate_dynamic_piano_chatter`（L1428，彈琴時的邊彈邊講）。

> ⚠️ `midi_sheets/midi_catalog.json` 目前是 `{}`（空的），曲庫靠 `songs_library/` — 而 **`songs_library/` 也只有一個空的 `cover_cache/`**。

### 8.3 唱歌 / 翻唱管線

**兩條路徑：**

**A. 即時唱歌** `singing_engine.py`（209 行）
```
Google/Tavily 搜歌詞 → Gemini 韻律調校 → edge-tts 合成 → pygame 播放
                     → 與鋼琴伴奏、VTS 口型同步
```
> ⚠️ **目前是孤兒模組** — 只被 `scratch/` 的測試引用，主程式改走路徑 B。

**B. 自動翻唱管線** `services/auto_cover_pipeline.py`（403 行）
```
download_youtube_audio()        yt-dlp 下載
  → separate_stems_gpu()        demucs 人聲/伴奏分離 (GPU)
  → pitch_shift_vocal_to_female()  自動偵測 pitch 並移調
  → neural_voice_converter.convert_vocal_to_xiaoyi()
        RVC：HuBERT 取特徵 + Net_G 解碼
        模型: services/rvc/*.pth + rmvpe (models/rvc)
  → mix_cover_and_export()      ffmpeg 混音
  → 副歌 30 秒裁切
```
- `produce_and_sing_cover()`：由工具 `sing_song` 觸發
- `master_7L_official_caomei.py`：**批次預製**3 首（Never Gonna Give You Up / 大魚 / 愛你），含 `chorus_start`/`chorus_dur` 裁切設定。**無 `__main__` guard，import 即執行**
- 訓練腳本：`train_xiaoyi_rvc.py`、`train_7l_tts_voiceprint.py`、`prep_xiaoyi_girl.py`、`record_clean_dad_voiceprint.py`
- 語料產生：`scripts/generate_full_corpus.py`（160+ 句高情緒語料）、`finetune_generate_corpus.py`（edge-tts 合成）、`finetune_run_training.py`（GPT-SoVITS 增量微調 s2→s1）

### 8.4 NDI 透明接收器

| 檔 | 用途 |
|---|---|
| `7L_NDI_Receiver.py`（507 行） | Live2D 主視窗透明疊加（Alpha 通道、無邊框、置頂、滑鼠穿透、60FPS） |
| `7L_Piano_NDI_Receiver.py`（739 行） | 鋼琴/Synthesia/SeeMusic 透明疊加，預設 980x260、支援裁切對話框、多音符著色 |

皆為 **PyQt6 + NDIlib**，支援 Hyper-V/VMware 跨機 NDI 自動搜尋。

---

## 9. 主動行為系統（讓 AI「像活的」）

| Worker | 行數 | 節奏 | 行為 |
|---|---|---|---|
| `proactive_worker` | 7473 | 45~75s 隨機 | 需 `silence_ticks >= 40`（40 秒沒人講話）且非說話/彈琴/唱歌中。先檢查前景視窗是否穩定（100% 變動就跳過，省 API） |
| `background_mind_stream_worker` | 7282 | 1.2~2.5s | **思緒流**：Gemini Live API（`gemini-3.1-flash-live-preview`）串流逐 token 生成內心獨白 → 抽 `[THOUGHT]` → `thought_memory`。失敗降級 `gemini-3.5-flash-lite` |
| `streamer_mind_loop_worker` | 6331 | 1s 檢查 | 讀 `STREAMER_MIND_BOARD` 未讀觀眾留言，空閒時用 3.1 Flash Lite → 3.5 → 3.7 梯隊**一次讀 100 句** |
| `live_timer_sensor_worker` | 4304 | 事件驅動 | `core/live_timer_sensor.py`：正規表示式解析「X 分鐘後提醒我…」→ tick 倒數 → 到點喊話 |
| `expression_keeper_worker` | 5731 | — | 維持表情（避免被覆蓋） |
| `autonomous_wander_worker` | 5842 | — | 模型自主遊走畫面 |
| `anti_watermark_worker` | 5761 | — | 防浮水印 |
| `cma_monitor_worker` | 7743 | — | CMA 狀態監控（`data/cma_status.json`） |

**睡眠模式** `IS_SLEEPING`：所有 worker 檢查此旗標就睡，**0 API 呼叫**，只認喚醒詞（起床/早安/嗨 etc.）。

**獎懲系統**：`execute_punish_action` / `execute_reward_action`（三級），可由 `/punish`、`/reward 1~3` 或 Web 觸發。

---

## 10. 即時任務廣播（`realtime_tasks/task_manager.py`，288 行）

`realtime_task_mgr` singleton，**雙寫**：
1. 本地 `data/realtime_tasks_local.json`
2. Firebase Firestore（collection `realtime_status`，doc `7L_current_task`）— 供外部儀表板讀取

狀態結構（實際檔案內容）：
```json
{
  "current_activity": "7L 閒著發呆 (無事啟動就緒)",
  "active_tasks": {},
  "piano_state": { "is_playing", "title", "progress_percent",
                   "instrument", "speed", "volume" },
  "vision_context": "螢幕上的字...",     ← 由 vision worker 更新
  "audio_context": "聲音現在沒有...",     ← 由 audio worker 更新
  "dad_context": { "last_input", "current_topic", "is_read", "read_time" },
  "audience_context": { "last_viewer", "last_comment", "is_read", ... }
}
```
API：`register_task` / `finish_task` / `update_activity` / `update_piano_state` / `update_vision_context` / `update_audio_context` / `start_dad_task` / `start_audience_task` / `get_realtime_summary()`（→ 注入主動發言 prompt）。

---

## 11. YouTube 陪伴模式（`services/yt_companion_service.py`，322 行）

```
find_youtube_window()          找 YouTube 視窗 (Win32)
  ├ video_worker: dxcam 截圖 → Gemini Live API (1 FPS)
  └ audio_worker: AudioLoopbackStreamer (WASAPI 16kHz)
       ↓
  EYE_EAR_SYSTEM_INSTRUCTION (L171)  「眼+耳」系統指示
       ↓
  YT_PERCEPTION_LOG (deque 15) → add_perception_memory()
       ↓
  record_vision_history_entry() → 視覺歷史 → 記憶
```
旗標：`IS_YT_WATCHER_ENABLED`（預設 True）、`IS_YT_COMPANION_ACTIVE`、`CURRENT_YT_TITLE`、`LATEST_YT_FRAME_BYTES`（供 Web 看最新幀）。

---

## 12. 配置與金鑰

### `.env`（主入口用，11 個 key）
```
DISCORD_TOKEN_7L  FIREBASE_CRED_JSON  GEMINI_API_KEY
GROQ_API_KEYS (16 個逗號分隔)  MONGO_URI  OPENROUTER_API_KEY
TAVILY_KEYS  SIGN_API_KEY  TIKTOK_USERNAME
MIC_ENERGY_THRESHOLD  MIC_MIN_RMS_THRESHOLD
```

### `6.env`（`bot.bat` 專用，8 個 key）
```
DISCORD_TOKEN, DISCORD_TOKEN_HATSUNE, _HORI, _KAORUKO, _MARIN, _NAZUNA
GEMINI_API_KEY, GROQ_API_KEYS, OPENROUTER_API_KEY
```

### 其他環境變數（程式碼中讀取）
| 變數 | 用途 | 預設 |
|---|---|---|
| `GROQ_TEXT_MODELS` | 覆寫模型梯隊 | `DEFAULT_MODEL_LADDER` |
| `TTS_ENGINE` / `TTS_FALLBACK` | TTS 引擎鏈 | `kokoro` / `edge,xiaoyi` |
| `TTS_ZH_VOICE` / `TTS_JA_VOICE` / `TTS_EN_VOICE` / `TTS_SPEED` | 聲線參數 | `zf_001` / `edge` / `af_sol` / `1.0` |
| `TTS_KOKORO_MODEL` / `TTS_KOKORO_VOICES` | 模型檔名 | `kokoro-v1.1-zh.onnx` / `voices-v1.1-zh.bin` |
| `RAG_GEMINI_EMBED_MODEL` | Gemini 嵌入模型 | `gemini-embedding-001` |

**依賴重點**（`requirements.txt` 51 行）：分 5 大類 — 平台通訊（discord/pyvts/TikTokLive）、LLM（groq/google-genai/tavily/ddg）、語音（kokoro-onnx/**misaki[zh]**/edge-tts/faster-whisper/soundcard）、RAG（chromadb/fastembed/faiss-cpu）、其他（firebase-admin/opencv/pyautogui/**mido**/dxcam/transformers）。
- ⚠️ **PyTorch 需另行安裝**（CUDA 12.4 專屬 index），requirements 不含
- ⚠️ `mido` 是**無條件 import**（`piano_engine` 模組層），缺了直接無法啟動（commit `84c6c74` 修）

---

## 13. Git 歷史解讀

14 個 commits 全在 **2026-09-22**（今天）：

```
13f1fcb Initial import: AI VTuber package        ← 專案首次進版控
38f2b71 fix: 移除硬編碼 C:\Users\qiwai 路徑
14a0697 chore: 重寫 requirements.txt + 清 llm_engine 死碼
b796cc9 feat: core/groq_router.py 文字路由
6d37400 feat: services/tts_router.py Kokoro 預設 TTS
9e5c829 feat: 新增 RAG
93d05cf feat: vts_7L_test 接入 Groq/TTS/RAG
6a7050d fix: RAG 去重與門檻校準
0071fd8 fix(groq): 移除不存在的 groq/compound[mini] + /models 交叉驗證
84c6c74 fix(deps): 補齊 V7 主路徑缺件
e1ddf26 fix: 還原被誤判死碼的 GROQ_CLIENTS + 明確路徑載 .env
161dc63 chore: 忽略運行期產物
7b7bec7 feat: Vercel Groq 轉發器 + VTS token 領取
57fee7b fix(tts): kokoro 用 misaki v1.1 修正粵語聽感  ← HEAD
```

**結論**：這是一次「把既有未版控專案匯入 git，同日連續補強」的過程。當日主力工作是：**導入 Groq 文字梯隊 + Kokoro 本地 TTS + RAG + Vercel 轉發器**，並修掉一批依賴與路徑問題。

未提交的 6 個 `data/*.json` 是 runtime 狀態（記憶、任務、曲目目錄），屬正常運行產物。

---

## 14. 問題清單與風險（依嚴重度）

### 🔴 高風險

1. **`execute_local_python_code`（L762）無沙箱** — LLM 可透過 function calling 執行任意本機 Python，僅靠 prompt 層約束。直播場景等於遠端可執行碼。
2. **金鑰明文且未加密** — `.env` / `6.env` 雖已 gitignore，但 16 個 Groq + Gemini + Firebase + TikTok 簽章 key 全在明文檔。
3. **`execute_local_python_code` + `web/api/tools/execute`（無鑑權）** — Web 控制台 port 7860 綁定不明（未見 `host=` 參數），若綁 0.0.0.0 則同網段任何人都能重啟/關機/執行工具/清空記憶。

### 🟠 中風險

4. **`check_env_keys.py` 硬編碼 `c:/Users/qiwai/.env`** — 當前使用者是 `CPXru`，此檔必然失敗（commit `38f2b71` 聲稱清掉硬編碼路徑，但漏了這檔）。
5. **多處殘留 `C:\Users\qiwai`**：`core/paths.py` `_LEGACY_USER`（有 fallback，尚可）、`scripts/finetune_*.py`、`scripts/generate_ciallo_mp3.py`、`scripts/test_ciallo_variations.py`、`scripts/test_short_cia.py`、`services/auto_cover_pipeline.py` 的 `C:\ffmpeg\bin\ffmpeg.exe`。**換機器即失效**。
6. **`setup.ps1` 從 `http://172.22.208.1:8888`（HTTP、內網 IP）下載 zip 並執行** — 無憑證驗證，且 fallback 到 `192.168.1.102`。
7. **`singing_engine.py` 是孤兒** — 主程式不引用，實際唱歌走 `auto_cover_pipeline`。但 `master_7L_official_caomei.py` 無 `__main__` guard，被 import 就會跑批次翻唱。

### 🟡 低風險 / 品質

8. **`services/llm_engine.py`（1,061 行）是死碼** — 全專案無任何 `from services.llm_engine import`。與 `core/llm_engine.py`（120 行）重複且已分叉（`KEYS_MIC_LIVE` / `KEYS_DAD_MAIN` 只存在於 services 版）。
9. **主檔 7,881 行單檔** — `vts_7L_test.py` 混雜輸入、輸出、LLM、工具、截圖、音訊、Discord、VTS 等所有職責，`from core.db import *`（L473）等 wildcard import 加劇耦合。
10. **空資料夾**：`knowledge/`（只有 README）、`7L_Playground/`（0 檔）、`songs_library/`（只有空 `cover_cache/`）、`midi_catalog.json = {}` — **RAG 與鋼琴曲庫實質上是空的**。
11. **`web/` 只有 `index.html`**，且 `index()` 在檔案不存在時回 fallback HTML — 單檔前端（32.5KB）承載全部 UI，無打包/壓縮。
12. **`memory_config.json` 的 `dialogue_capacity: 0`** — 對話記憶容量設為 0，可能導致對話記憶不保留（需確認 `load_memory_capacity_setting()` 對 0 的解讀）。
13. **`vts_token.txt` 在 gitignore，但 `.bak` 也在 ignore** — token 本身仍存在於磁碟明文。
14. **無任何自動化測試** — `scratch/` 60 個測試檔全是手動腳本，非 pytest。
15. **兩份 Discord bot 舊檔**（`7L.py` 1,891 行、`bot.py` 319 行）與主系統並存，職責邊界不清。

---

## 15. 心智模型總圖

```
                    ┌──────────────── 輸入 ────────────────┐
   麥克風(STT) ─┐
   TikTok 彈幕 ─┤   ┌──────────────────────────────┐
   鍵盤/檔案  ─┼──→│  input_queue (asyncio.Queue)  │
   Web 控制台 ─┤   └──────────────┬───────────────┘
   Live 事件  ─┘                  ↓
                        chat_processor_worker
                    ┌─────────────┴──────────────┐
                    ↓ (爸爸)            ↓ (觀眾)
          process_chat_message    STREAMER_MIND_BOARD
                    ↓                     ↓
     ┌── 記憶需求判定 (5 級) ──┐   streamer_mind_loop
     │  RAG 檢索 (chroma)     │   (批量 100 句)
     │  Firestore 歷史        │            ↓
     │  雲端知識 + 觀眾檔案    │      (併入 prompt)
     └──────────┬────────────┘
                ↓
   PromptTemplateEngine.build_chat_system_prompt
   (時間+Tick / 鋼琴狀態 / OS 遙測 / 記憶 / 硬規則 / 動作標記)
                ↓
   ┌────────── fetch_ai_response ──────────┐
   │ 梯隊1: Groq (16 keys, 4 模型, /models 驗證) │
   │ 梯隊2: Gemini (6 功能分區金鑰池 + 雙熱待命)  │
   │ 梯隊3: Vercel 轉發器 (28 keys, 逐 key 冷卻)  │
   └──────────────────┬───────────────────┘
                      ↓ bot_reply（含 [EXPRESSION] [SPEED] [THOUUGHT]...）
        ┌─────────────┼──────────────┐
        ↓             ↓              ↓
  execute_actions  speech_queue   save_to_long_term
   → VTS 表情/口型    ↓             (Firestore)
                 play_voice_complete
                   ├ TextCleanEngine (多音字/斷句/抽獨白)
                   ├ tts_router: kokoro → edge → xiaoyi
                   ├ sentence-level 情緒導演 (Gemini Live)
                   └ execute_action_timeline → VTS 同步
                          ↓
                 pygame 播放 + 字幕 + Web 廣播

  ══════════ 背景自主行為 ══════════
  思緒流(1.2s) → thought_memory → 回到 prompt
  主動發言(45~75s，需靜默 40s) → speech_queue
  鋼琴自動演奏 / 翻唱管線 / YouTube 陪伴 / 周邊視覺
```

---

## 16. 若要接手，建議的優先順序

1. **先把 `execute_local_python_code` 關掉或加白名單**（改 `DEFAULT_TOOLS_CATALOG` 移除 + prompt 層禁用）
2. **確認 7860 端口綁定位址**，非必要不綁 0.0.0.0
3. **清理 `C:\Users\qiwai` 殘留**（`check_env_keys.py` 最容易踩）
4. **刪掉或合併 `services/llm_engine.py`**（1,061 行死碼，且與 `core/` 版本分叉）
5. **補 `knowledge/` 實際知識檔並重跑 `ingest_rag.py`**，否則 RAG 只在撈記憶
6. **把 `vts_7L_test.py` 拆層**：輸入源 / 分派 / LLM / 輸出 各自獨立，才好測試
7. **`memory_config.json` 的 `dialogue_capacity: 0`** 先確認是否為預期
8. 加 `pytest` + 幾個純函式測試（`TextCleanEngine`、`evaluate_memory_demand`、`parse_action_timeline` 最容易先測）

---

*本報告僅由靜態閱讀產生，未執行專程式；所有行號以 2026-09-22 的 working tree 為準。*
